#include "main.h"
/**
 * _abs - Prints the absolute value of an integer
 * @n: The number whose absolute value will be computed
 *
 * Return: The absolute value of the argument
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
